# Music_Player
A browser-based audio player that loads local files/folders, manages playlists (create, reorder, shuffle, repeat), and provides smooth playback controls with a responsive UI, all working fully offline.
